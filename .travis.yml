dist: xenial

language: python

python:
    - 3.5
    - 3.6
    - 3.7

install:
    - pip install -r requirements_dev.txt
    - pip install --editable .
    - pip freeze

script:
    nosetests

notifications:
    email: true
